def modules = [:]
pipeline {
	agent any
	stages {
		stage('Test'){
			steps{
				checkout scm
				script{
					module.first = load "first.groovy"
					module.first.buildDocker()
				}
			}
			
		}
	}
}

